# توثيق هيكلية تطبيق Enjaz One

يتبع هذا التطبيق معمارية **System-Based Architecture**، حيث يتم تقسيم الميزات إلى "أنظمة" (Systems) مستقلة منطقياً تحتوي على مكوناتها وخدماتها الخاصة.

## 1. الهيكل العام (App Shell)
المكونات التي تظهر في جميع الصفحات وتدير التنقل.

| المكون | المسار | الغاية | العلاقات |
| :--- | :--- | :--- | :--- |
| **App (Root)** | `App.tsx` | مدير الحالة والموجه الرئيسي (Router). يتحكم في أي صفحة يتم عرضها حالياً بناءً على الحالة `currentView`. | يستدعي `Header`, `Sidebar` ويقوم بتبديل المحتوى الرئيسي (Dashboard, Operations, etc). |
| **Sidebar** | `systems/app-shell/components/Sidebar.tsx` | القائمة الجانبية للتنقل بين الأنظمة الرئيسية. | يغير حالة `currentView` في `App.tsx`. يحتوي على قائمة المستخدم للوصول لـ `Profile` أو `Logout`. |
| **Header** | `systems/app-shell/components/Header.tsx` | الشريط العلوي، يحتوي على التنبيهات، زر الإنشاء السريع، وعنوان الصفحة. | يفتح `CreateWorkItemModal`، يعرض `Notifications`، ويبدل العرض إلى `Settings`. |

---

## 2. الأنظمة الرئيسية (Main Systems)

### 📊 نظام لوحة المعلومات (Dashboard System)
| الصفحة | المسار | الغاية | الارتباطات |
| :--- | :--- | :--- | :--- |
| **Dashboard** | `systems/dashboard/components/Dashboard.tsx` | عرض إحصائيات عامة، رسوم بيانية للحالة (مفتوح، منجز)، وأرقام المهام الحرجة. | تعرض بيانات ملخصة فقط. لا تحتوي على تفاعلات معقدة سوى عرض البيانات. |

### ⚙️ نظام العمليات (Operations System)
القلب النابض للتطبيق لإدارة المهام والبلاغات.

| الصفحة | المسار | الغاية | الارتباطات |
| :--- | :--- | :--- | :--- |
| **WorkItemList** | `systems/operations/WorkItemList.tsx` | عرض قائمة بجميع العمليات (مهام، بلاغات، صيانة) مع فلاتر وبحث. | عند النقر على عنصر، يتم فتح **WorkItemDetail**. |
| **WorkItemDetail** | `systems/operations/WorkItemDetail.tsx` | (Modal) عرض تفاصيل العملية، التعليقات، سلسلة الموافقات، والتحليل الذكي (AI). | يرتبط بـ `CreateWorkItemModal` للتعديل، ويعرض `AiAnalysisCard`. |
| **ApprovalsView** | `systems/operations/ApprovalsView.tsx` | مركز الموافقات. يعرض الطلبات التي تحتاج موافقة المستخدم الحالي. | عند النقر، يفتح **WorkItemDetail** لاتخاذ قرار (قبول/رفض). |
| **CreateWorkItemModal** | `systems/operations/components/CreateWorkItemModal.tsx` | نافذة منبثقة لإنشاء مهمة أو بلاغ جديد. | يتم استدعاؤها من `Header` أو `ProjectsListView`. |

### 🏗️ نظام المشاريع (Projects System)
| الصفحة | المسار | الغاية | الارتباطات |
| :--- | :--- | :--- | :--- |
| **ProjectsListView** | `systems/projects/ProjectsListView.tsx` | عرض بطاقات المشاريع وملخص مالي وزمني لكل مشروع. | النقر على مشروع يفتح صفحة **ProjectDetail**. |
| **ProjectDetail** | `systems/projects/ProjectDetail.tsx` | صفحة تفصيلية للمشروع تحتوي على تبويبات داخلية. | تحتوي على تبويبات فرعية: نظرة عامة، العمليات (WorkItems)، الوثائق، الأصول، الفريق، والإعدادات. |

### 📱 نظام العمليات الميدانية (Field Ops System)
| الصفحة | المسار | الغاية | الارتباطات |
| :--- | :--- | :--- | :--- |
| **FieldOps** | `systems/field-ops/components/FieldOps.tsx` | واجهة مخصصة للجوال لرفع البلاغات الميدانية، استخدام الكاميرا، والموقع الجغرافي، ودعم العمل دون إنترنت (Offline). | تقوم بإنشاء `WorkItem` جديد وتخزينه محلياً أو رفعه للسيرفر. |

### 📚 نظام المعرفة (Knowledge System)
| الصفحة | المسار | الغاية | الارتباطات |
| :--- | :--- | :--- | :--- |
| **KnowledgeBase** | `systems/knowledge/components/KnowledgeBase.tsx` | قاعدة بيانات للإجراءات والسياسات الداخلية (Wiki). | تحتوي على **AiWikiAssistant** للإجابة على أسئلة الموظفين بناءً على المقالات. |

### 📦 نظام الأصول (Assets System)
| الصفحة | المسار | الغاية | الارتباطات |
| :--- | :--- | :--- | :--- |
| **AssetsView** | `systems/assets/components/AssetsView.tsx` | سجل المعدات والأصول، حالتها، وموقعها. | يفتح نوافذ منبثقة لـ "نقل العهدة" أو "طلب صيانة" (يُنشئ `WorkItem`). |

### 📂 نظام الوثائق (Documents System)
| الصفحة | المسار | الغاية | الارتباطات |
| :--- | :--- | :--- | :--- |
| **DocumentsView** | `systems/documents/components/DocumentsView.tsx` | مكتبة مركزية لكل الملفات والمخططات والعقود. | تدعم التحليل الذكي للمستندات عبر Gemini AI. |

### 👤 الملف الشخصي والإعدادات
| الصفحة | المسار | الغاية | الارتباطات |
| :--- | :--- | :--- | :--- |
| **ProfileView** | `systems/profile/components/ProfileView.tsx` | عرض أداء الموظف، والعهد التي بحوزته. | ترتبط بـ `AssetsView` لعرض تفاصيل العهدة. |
| **SettingsView** | `systems/settings/components/SettingsView.tsx` | إعدادات النظام، قواعد الأتمتة (Automation Rules)، وإعدادات الذكاء الاصطناعي. | تتحكم في سلوك `automationService`. |

---

## 3. تدفق البيانات (Data Flow)

1.  **المخازن (Repositories):** توجد في `shared/services/*Repo.ts`. هي المسؤولة عن جلب وحفظ البيانات (حالياً تستخدم LocalStorage والمحاكاة).
2.  **الحالة (State):** يتم رفع الحالة إلى `App.tsx` عبر الخطاف `useEnjazCore` لتوزيع البيانات على الأنظمة المختلفة.
3.  **التحديثات:** عند تحديث بيانات (مثلاً "قبول طلب")، يتم استدعاء دالة في الـ Repo، ثم إعادة تحميل البيانات في `App.tsx` لتنعكس التغييرات في كل الشاشات فوراً.
